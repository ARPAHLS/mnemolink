name: Sync Labels

on:
  push:
    branches: [ "main" ]
    paths:
      - ".github/labels.yml"
      - ".github/workflows/labels.yml"
      - "scripts/sync_labels.py"
  pull_request:
    branches: [ "main" ]
    paths:
      - ".github/labels.yml"
      - ".github/workflows/labels.yml"
      - "scripts/sync_labels.py"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  sync:
    name: Synchronize Labels
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install pyyaml

      - name: Validate and Dry Run (Pull Request)
        if: github.event_name == 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/sync_labels.py --dry-run

      - name: Run sync_labels.py (Push to Main or Manual Dispatch)
        if: github.event_name != 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/sync_labels.py
